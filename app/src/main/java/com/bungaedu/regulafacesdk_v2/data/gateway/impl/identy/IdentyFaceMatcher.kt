package com.bungaedu.regulafacesdk_v2.data.gateway.impl.identy

import com.bungaedu.regulafacesdk_v2.data.gateway.FaceMatcher
import com.bungaedu.regulafacesdk_v2.data.model.FaceImage
import com.bungaedu.regulafacesdk_v2.data.model.Similarity

/*class IdentyFaceMatcher : FaceMatcher {
    override suspend fun compare(a: FaceImage, b: FaceImage): Similarity {
        // 🔹 Llamada real a Identy para comparar
        // Ejemplo:
        // val score = IdentySDK.compareFaces(a.bytes, b.bytes)
        // return Similarity(score, "$score%")

        // Mock de prueba
        val percent = (70..99).random()
        return Similarity(percent.toFloat(), "Comparado con Identy")
    }
}*/
